class Info:
	
	def __init__(self, engineVersion, criteriaVersion, clientMaxAssessments, maxAssessments, currentAssessments, messages):
		self.engineVersion = engineVersion
		self.criteriaVersion = criteriaVersion
		self.clientMaxAssessments = clientMaxAssessments
		self.maxAssessments = maxAssessments
		self.currentAssessments = currentAssessments
		self.messages = messages